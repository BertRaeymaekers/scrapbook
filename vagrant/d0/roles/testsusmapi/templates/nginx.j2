server {
    listen {{http_server_port|default(80)}};
    server_name {{http_server_name|default("_")}};

    location / {
        include         uwsgi_params;
        uwsgi_pass      unix:~{{susmapi_user}}/{{susmapi_name}}.sock;
    }
}
